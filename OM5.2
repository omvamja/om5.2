#include<stdio.h>
#include<conio.h>
void main (){
int a,b,c;
clrscr();

printf("eanter is a=");
scanf("%d",&a);
printf("eanter is b=");
scanf("%d",&b);
printf("eanter is c=");
scanf("%d",&c);

if (a<b){
     if (a<c){
     printf("minimum number is  a=%d",a);}
     else{
     printf("minimum number is c=%d",c);}

     }else{ if(b<c){
     printf("minimum number is b=%d",b);}
     else{
     printf(" minimum number is c=%d",c);
     }
     }


getch();}